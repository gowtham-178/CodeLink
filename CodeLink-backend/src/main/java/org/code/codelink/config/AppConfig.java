package org.code.codelink.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder config — BCryptPasswordEncoder and AuthenticationManager
 * are now declared in SecurityConfig.
 */
@Configuration
public class AppConfig {
    // intentionally empty — beans live in SecurityConfig
}
